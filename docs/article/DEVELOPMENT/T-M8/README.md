# T-M8 — Pre-writing revision: article-facing documentation and framing

Documentation-side scope for the 2026-07-22 pre-writing revision (specs in
`docs/article/REVIEW/`). Holds the label-conditional metric-family Remark
(structural vs label-aware `d_I`), the capability-matrix main figure and the
usefulness reframing, the A1–A4 practitioner-motivation paragraphs, and the
reproducibility-artifact assembly. Sister scope T-M7 holds the code-side tasks;
several T-M8 tasks consume T-M7 outputs (final numbers, caches). Executed as
session S7 (`../SESSIONS.md`).
